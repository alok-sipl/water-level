<% if(Object.keys(suppliers).length){
  for (var key in suppliers) { %>
<div class="col-lg-4">
  <div class="hpanel hgreen contact-panel">
    <div class="panel-body">
      <img alt="<%= (suppliers[key]['company_name'] != undefined) ? suppliers[key]['company_name'] : '' %>" class="img-circle m-b"
           src="<%= (suppliers[key]['compnay_image'] != undefined) ? suppliers[key]['compnay_image'] : sails.config.base_url + 'images/profile.png' %>">
      <h3><%= (suppliers[key]['company_name'] != undefined) ? suppliers[key]['company_name'] : '' %></h3>
      <h4><%= (suppliers[key]['name'] != undefined) ? suppliers[key]['name'] : '' %></h4>
      <div class="text-muted font-bold m-b-xs">
        <%= (suppliers[key]['city_name'] != undefined) ? ((suppliers[key]['area'] != undefined) ? suppliers[key]['area'] + ', ' : '') + suppliers[key]['city_name'] + ', ' + suppliers[key]['country_name'] : '' %>
      </div>
      <p><%= (suppliers[key]['mobile_number'] != undefined) ? suppliers[key]['mobile_number'] : '' %></p>
      <p><%= (suppliers[key]['email'] != undefined) ? suppliers[key]['email'] : '' %></p>
    </div>
    <div class="panel-footer contact-footer">
      <div class="row">
        <div class="col-md-6 border-right">
          <div class="contact-stat">
            <span>Status </span>
            <strong><% if(suppliers[key]['is_deleted']){ %>
              <a data-tooltip="" title="" data-status="true" data-url="<%= sails.config.base_url %>supplier/updateStatus/<%= key %>" class="button status-action active" data-id="<%= key %>" href="javascript:void(0);" data-original-title="Make Active"><i class="fa fa-check-square-o"></i></a>
              <% }else{ %>
              <a data-tooltip="" title="" data-status="false" data-url="<%= sails.config.base_url %>supplier/updateStatus/<%= key %>" class="button status-action active" data-id="<%= key %>" href="javascript:void(0);" data-original-title="Make In Active"><i class="fa fa-square-o"></i></a>
              <% } %>
            </strong>
          </div>
        </div>
        <div class="col-md-6 border-right">
          <div class="contact-stat"><a href="<%= sails.config.base_url %>supplier/edit/<%= key %>"
                                       title="<%= (suppliers[key]['name'] != undefined) ? suppliers[key]['name'] : '' %>">Edit
              Supplier</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<% }
} %>
